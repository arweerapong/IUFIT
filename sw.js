/* IUFIT service worker — Vue build
   ปล่อยอัปเดตแอปทีไร ให้ขยับเลขเวอร์ชันใน CACHE ทุกครั้ง (v1032 -> v1033 ...)
   ไม่ขยับ = เครื่องที่ติดตั้งไว้แล้วจะยังกิน cache ชุดเก่า

   กลยุทธ์ (เหมือน sw.js ของแอปจริง IUFIT-app-deploy/sw.js เป๊ะ):
     HTML/navigate = network-first  (ออนไลน์ได้ตัวล่าสุดเสมอ · ออฟไลน์ค่อยใช้ cache สำรอง)
     asset         = cache-first    (เร็ว · miss แล้วค่อยยิงเน็ตแล้วเก็บลง cache)

   ⚠️ ต่างจากของเดิมตรงเดียว: บิลด์ Vite ตั้งชื่อไฟล์ JS/CSS แบบมี hash (`/assets/xxx-a1b2.js`)
   จึง **precache ล่วงหน้าไม่ได้** เพราะชื่อเปลี่ยนทุกบิลด์ → precache เฉพาะ "เปลือกแอป"
   (index/manifest/icon) ที่ชื่อคงที่ ส่วน bundle ปล่อยให้ cache-first เก็บเองตอนโหลดครั้งแรก
   ผลลัพธ์ปลายทางเท่ากัน: เปิดครั้งแรกออนไลน์ 1 รอบ → ครั้งต่อไปเปิดออฟไลน์ได้

   ⚠️ cache-first ปลอดภัยกับข้อมูลสด เพราะ:
     • ยัดลง cache เฉพาะ response ที่ `sameOrigin` เท่านั้น
     • Firebase RTDB (GET) / worker (POST) เป็น cross-origin → ไม่เคยถูกเก็บ → ได้ของสดเสมอ
     • ไฟล์ที่ hash ในชื่ออยู่แล้ว บิลด์ใหม่ = ชื่อใหม่ = ไม่มีทางกินของเก่า
*/
/* v1033 = บิลด์ที่หั่น bundle ใหม่ (vendor แยกก้อน + IU MATE/ทัวร์ lazy)
   ขยับเวอร์ชันเพื่อให้เครื่องที่ติดตั้งไว้แล้วทิ้ง cache ก้อน index-*.js เดิม (734KB)
   ที่ไม่มีใครขอใช้อีกแล้ว — ชื่อไฟล์ที่มี hash ยังคงถูกเก็บตอน runtime เหมือนเดิม
   (FILES ด้านล่างไม่มี path ที่มี hash เลย จึงไม่ต้องแก้ตามทุกบิลด์) */
/* v1035 = รวมระบบแจ้งเตือน PWA + Capacitor เป็นชุดเดียว (services/push.ts)
   ไฟล์นี้เพิ่ม `pushsubscriptionchange` ซึ่งเป็นโค้ดใหม่ใน SW ⇒ ต้องขยับเวอร์ชัน
   ไม่งั้นเครื่องที่ติดตั้งไว้แล้วจะยังรัน SW ตัวเก่าที่ไม่มี handler นี้ */
/* v1036 = audit fix รอบสุดท้ายก่อน launch: แก้ปุ่ม "ส่งแผน" หน้าท่าฝึก (WorkoutView)
   จาก toast หลอกให้ยิง coach.sendPlan จริง + ลบกับดักคอมเมนต์ pushOn/pushOff
   ขยับเวอร์ชันเพื่อให้เครื่องที่ติดตั้งไว้แล้วรับ bundle ใหม่ */
/* v1037 = UI batch 2569-07-22: ปุ่มกู้บัญชีจัดวางใหม่ · ปฏิทินวันเกิดเปิดโหมดปี+จำกัด 90 ปี ·
   การ์ดทัวร์ใหญ่ขึ้น + แก้ข้อความ (2 ส่วน/นาฬิกา iOS) · IU Mate chat อธิบายปุ่มทีละแท็บ + พาทัวร์ */
/* v1038 = เมนู "ธีม"→"โหมดมืด" · อัปเดตศูนย์ช่วยเหลือ (help.ts) ให้ครบทุกฟีเจอร์จริง
   (เพิ่มแจ้งเตือน/LINE/นาฬิกา/Progress Report/คอร์ส PT/เทมเพลต/CSV) + ตัดข้อความเกินจริง */
/* v1039 = กรอบสแกน QR เป็นสี่เหลี่ยมจัตุรัส · แก้โฟลว์โค้ช onboarding: "ข้ามไปก่อน" บนหน้า
   ยืนยันอีเมล → ไปหน้าจบ (เดิมวนกลับฟอร์มโปรไฟล์โค้ช) · ปุ่มหน้ายินดีต้อนรับไม่ชิดขอบล่าง */
/* v1040 = ปุ่มยินดีต้อนรับไม่ชิดล่างจริง (auto-margin แทน justify-center ที่กิน padding) ·
   ตั้งค่า: ลบแถว "สำรองข้อมูล" ซ้ำ · หน้า /backup: ยืนยันอีเมลเด่น สำรองเองเล็กลง ·
   ไอคอนแท็บ "ลูกเทรน" เปลี่ยนจากคนวิ่งก้าง ๆ → ไอคอนคนเดี่ยวสะอาด */
/* v1041 = ปุ่มชิดล่าง แก้ต้นเหตุจริง: TWA/Android edge-to-edge รายงาน env(safe-area-bottom)=0
   ปุ่มไปอยู่ใต้แถบ gesture ⇒ ใส่ --iu-safe-b-min:24px กลางใน base.css (ทุกหน้า chromeless ได้) ·
   หน้ารับสิทธิ์ Trainer Pro: ตัด "เหมือนผู้ใช้ทั่วไป" + ตัดวงเล็บซ้ำที่นั่ง/ประวัติใน proSkipNote */
/* v1042 = หน้า "ยังไม่ได้เป็นลูกเทรน": ปุ่ม "สแกน QR" เปิดกล้องทันที · "อ่าน QR จากรูป"
   เด้งเลือกรูปทันที (เดิมทั้งสองเปิดชีตให้เลือกซ้ำ) — เพิ่ม prop autoStart ให้ QrScanSheet */
/* v1043 = ออดิตโหมดโค้ชทั้งเซตเทียบต้นฉบับ: ปุ่ม "แชท" เปิดหน้าแชทฮับจริง (ไม่ใช่หน้าลูกเทรน) ·
   "คำนวณ" เป็นชีตบนหน้าโฮม กดบันทึกค้างไว้คำนวณคนต่อไป ปิดแล้วกลับโฮม · "เพิ่มลูกเทรน" QR บนโฮม ·
   การบ้าน "แชทฟีดแบ็ก" เปิดห้องแชทในที่ · แก้ leaderboard ไม่ส่ง code · เพิ่ม back-guard ทุกชีตโค้ช
   ⇒ ทุกหน้าต่าง ปิด/กดกลับ = กลับที่เดิมก่อนเปิด */
/* v1044 = โค้ชเห็นลูกเทรนใหม่ไวขึ้น (poll members ทุก 8 วิ + sync ตอน foreground · ไม่ใช่ SSE) ·
   การ์ดการบ้านรายคน = 1 คนหลายรายการเหมือนต้นฉบับ (กางการ์ดคนที่โฟกัส) ·
   ตัด "· รายงาน" ในป้าย "ดูรายละเอียดทั้งหมด" (สร้าง Progress Report เหลือปุ่มใหญ่ที่เดียว) */
/* v1045 = onboarding โค้ช: กด "ไม่สร้างโปรไฟล์ส่วนตัว" แล้วไปหน้ายืนยันอีเมลตรง ๆ
   เอากล่องที่สอง "ไม่ต้องรีบ ทำทีหลังได้" ออก (รายละเอียดบอกครบแล้วในกล่องคำถามเอง) */
/* v1046 = Result Card เปิดใช้จริง: การ์ดฉลอง 5 แบบ (streak/workout/PR/progress/monthly)
   เด้ง overlay ตอนทำสำเร็จ + มาสคอต bob + confetti + ปุ่มแชร์ PNG · ตัวจับ latch กันเด้งซ้ำ */
/* v1106 = รวมพลังงานสองระบบเป็นถังเดียว: แคลจากนาฬิกาหักเข้าโควตา "คงเหลือ" ได้แล้ว
   (บวกเฉพาะส่วนที่เกินค่ากิจกรรมที่ tdee จ่ายไว้ · นับทางเดียว ไม่ซ้ำกับเซสชันที่บันทึกเอง
   ดู lib/energyBudget.ts) · IU MATE ตอบเลข "เหลือกี่แคล" ชุดเดียวกับการ์ดหน้าแรกแล้ว
   🐛 กู้การ์ดยืนยันกิจกรรมจากนาฬิกาที่หายไปตอนย้ายหน้า /watch ⇒ เซสชันกีฬากลับเข้า user.ex
      (สตรีค/เหรียญ/จำนวนเวิร์กเอาต์กลับมานับ) · แถบนาฬิกาหน้าแรกตามวันที่เลือกแล้ว
   🧹 ลบ ResWatchSlide/ResDrillWatch ที่กลายเป็นไฟล์กำพร้า + เลิก prefetch จอที่กดเข้าไม่ได้
   ✏️ ปุ่ม/ทางเข้ากลับไปใช้คำว่า "เชื่อมนาฬิกา" (มีสองแหล่งแล้ว ไม่ใช่ Health Connect ทางเดียว)
   🤖 IU MATE: ถาม "โปรตีนวันละกี่กรัม" ได้ตัวเลขของตัวเองเลย ไม่ใช่บทความ + ปุ่มให้กดต่อ */
/* v1107 = ⚡ ความเร็วตอนเปิดแอป (ฟีดแบ็ก "ช้ากว่าตอนเป็น vanilla")
   วัดจาก dist จริง: โหลดน้อยกว่า vanilla 3 เท่า (264KB gz เทียบ 853KB) แต่ช้ากว่า
   เพราะต้องผ่าน **5 ชั้นแบบต่อคิว** กว่าจะเห็นหน้าแรก — ปัญหาคือจำนวนรอบ ไม่ใช่ไบต์
     1. HTML: network-first → แคชก่อนแล้วอัปเดตเบื้องหลัง
        (ของเดิมรอเน็ตครบรอบก่อนโค้ดบรรทัดแรกจะรัน ทุกครั้งที่เปิดแอป
         แอป Android ยิ่งหนักเพราะ server.url ชี้ที่ iufit.com)
     2. precache ก้อน /assets ที่ index.html อ้าง ตั้งแต่ตอน install
        (เดิมเก็บแบบ "เจอตอนไหนค่อยเก็บ" ⇒ ครั้งแรกหลังอัปเวอร์ชันต้องยิงทีละก้อนต่อคิว)
     3. modulepreload ก้อนหน้าแรก 17 ไฟล์ ⇒ ยุบ 3 ชั้นเหลือ 1 (ดู scripts/vite-preload-landing.mjs) */
/* v1108 = 🐛 "จับคู่นาฬิกาไว้แล้วแต่ไม่มีให้เชื่อมเรือนเดิม"
   ชีตเลือกแหล่งเรียก startScan() เสมอ ⇒ ทางเดียวที่มีคือไปจับคู่เรือนใหม่
   ตอนนี้เคยจับคู่แล้วจะเจอปุ่ม "เชื่อมนาฬิกาเรือนเดิม" เป็นทางหลัก (เชื่อม+ดึงข้อมูลในคำสั่งเดียว)
   ส่วน "จับคู่เรือนอื่น" ลดชั้นเป็นลิงก์รอง · เชื่อมไม่ติดบอกเหตุผลจริง ไม่เด้งไปสแกนเงียบ ๆ
   ✏️ คำอธิบายแถวตั้งค่าเปลี่ยนเป็น "อุปกรณ์ที่รองรับ: …" บอกยี่ห้อนาฬิกาแทนชื่อแอปตัวกลาง */
/* v1109 = 🐛 "เชื่อมเรือนเดิมแล้วขึ้นว่าไม่พบนาฬิกา" — เชื่อมติดจริงแต่แอปบอกผิด
   `watchSync` คืน 'empty' เมื่อ **เชื่อมได้แต่ยังไม่มีข้อมูล** ส่วนตารางของสายสแกน
   แปล 'empty' ว่า "ไม่พบนาฬิกา" ⇒ ผมใช้ตารางเดียวกันทั้งสองสายเลยแปลผิด
   ตอนนี้สายซิงก์มีตารางของตัวเอง · 'empty' นับเป็นสำเร็จ (บอกว่ายังไม่มีข้อมูลใหม่)
   ⌚ เชื่อมสำเร็จแล้ว **นาฬิกาสั่น 1 ครั้ง** เป็นหลักฐานว่าคุยกับเรือนนั้นได้จริง
      (มีคำสั่งหยุดสั่นใน finally เสมอ — สั่นค้างจนแบตหมดผู้ใช้แก้เองไม่ได้)
   ✏️ ป้าย "กำลังซิงค์…" บอกแหล่งด้วย และ 'iufit-band' แสดงเป็น "IUFIT Band"
      (เดิมโชว์คีย์ดิบ ๆ และไม่บอกว่าซิงก์กับอะไร ⇒ เข้าใจผิดว่าแอบไป Health Connect) */
/* v1110 = 🐛 "ฉลองครบเป้าน้ำไม่เด้ง"
   ตัวเช็กเคยอยู่ใน wrapper ของหน้าแรก ⇒ มีแค่ปุ่มน้ำ 4 ปุ่มบนหน้านั้นที่ทำให้เด้ง
   หน้าน้ำ /water (คนกดมากที่สุด) · การ์ดทำอะไรต่อดี · หน้าประวัติ · IU MATE · LINE ข้ามหมด
   ⇒ ย้ายตัวจับไปที่ `daily.setWater()` ซึ่งเป็นประตูเขียนน้ำเดียวจริง ๆ
      (บทเรียนเดียวกับ wearIngest: ตัวจับอยู่ที่ประตู ไม่ใช่ที่ปุ่ม)
   จับเฉพาะ **ขอบขาขึ้น** (ต่ำกว่าเป้า → ถึงเป้า) ไม่งั้นกดต่อหลังครบจะเด้งซ้ำทุกครั้ง
   หน้าน้ำได้อนิเมชันฉลองด้วยแล้ว (เดิมมีแต่หน้าแรก) + เทส 9 เคสคุมทุกทางเข้า */
/* v1111 = 🐛 "ตัดการเชื่อมต่อนาฬิกาแล้วยังค้าง ขึ้นกำลังซิงค์ แล้วเชื่อมกลับให้เอง"
   `wearDisconnectDo()` เดิมลดธงอย่างเดียว ไม่ได้บอกฝั่งนาฬิกาสักคำ ⇒ หลังกดตัด
   ตัวซิงก์อัตโนมัติยังยิงต่อ (ด่านเดิมดูแค่ "ยังจับคู่อยู่ไหม") → กำลังซิงค์ค้าง →
   เชื่อมติดปุ๊บ `markWatchConnected()` ตั้ง wearOn กลับเป็น true
   ⇒ **แอปยกเลิกคำสั่งตัดของผู้ใช้เอง** · และสะพาน IUFIT_WEAR_BRIDGE ค้าง
   ทำให้เส้นทางซิงก์ของ Health Connect ยังวิ่งมาลงแบนด์ (ที่เห็นว่า "ไปเข้า HC เอง")
   ตอนนี้: wearOn=false คือคำสั่งหยุด ทุกเส้นทางอัตโนมัติเคารพ · ถอดสะพาน · สั่งตัด BLE จริง
   ⚠️ ไม่ลบ MAC — "ตัดการเชื่อมต่อ" คนละเรื่องกับ "เลิกจับคู่" กลับมากดเชื่อมเรือนเดิมได้เลย */
/* v1112 = ⌚ ปุ่ม "แก้ไขชื่อนาฬิกา" (เดิมตั้งชื่อได้ครั้งเดียวตอนจับคู่ · พิมพ์ผิดต้องจับคู่ใหม่)
   ⚠️ รอบนี้ต้องลง APK ใหม่ด้วย — แก้ที่ฝั่ง native: `isConnected()` เคยเชื่อตัวแปรที่เราตั้งเอง
   ซึ่งค้างเป็น "เชื่อมอยู่" เมื่อ BLE หลุดโดยไม่มี callback ⇒ แอปขึ้นว่าเชื่อมแล้วแต่
   ไม่สั่น · หัวใจไม่ขึ้น · แบตค้างค่าเก่า (ทั้งสามอาการมาจากรากเดียวกัน)
   ตอนนี้ถาม SDK เป็นคำตอบสุดท้าย + ล้างสถานะค้างให้เอง
   🐛 และ `connect()` เป็นคำสั่งเดียวที่ไม่เคยมีเพดานเวลา (คำสั่งอ่านได้ once() ไปหมดแล้ว)
      BLE เงียบหายได้โดยไม่มี callback ⇒ Promise ค้างตลอดกาล ⇒ จอค้าง "กำลังซิงค์…"
      และรอบที่ค้างทิ้งลิงก์ครึ่งใบไว้กวนรอบถัดไป = อาการ "ติด ๆ ดับ ๆ"
      ตอนนี้เพดาน 20 วิ + ตัดลิงก์ทิ้งให้สะอาดทุกครั้งที่ล้มเหลว + เหตุผลใหม่ connect_timeout */
/* v1113 = ⌚ **วัดชีพจรสดใช้ได้จริงแล้ว** + 🐛 ฉลองน้ำ (รอบสอง) + แถบซิงก์เงียบลง
   ชีพจรไม่เคยขยับเพราะสองชั้นซ้อนกัน ทั้งคู่อยู่ฝั่ง native:
     1. `startHeart` **ขาด `@PluginMethod`** ⇒ Capacitor ไม่เคย expose · เรียกจากเว็บ
        ได้ผลเป็น "method not implemented" มาตั้งแต่วันแรก
     2. ค่าส่งเป็น event (`notifyListeners`) ซึ่งไปไม่ถึงหน้าเว็บที่โหลดจากรีโมต
        (พิสูจน์แล้วตอนสแกน: deviceFound ไม่เคยยิงถึง) ⇒ เปลี่ยนเป็น poll ทุก 1.5 วิ
   + ปุ่ม "วัดชีพจรตอนนี้" บนหน้านาฬิกา · หยุดเองใน 1 นาที · ออกจากหน้าแล้วหยุดเสมอ
   🐛 ฉลองน้ำยังไม่เด้ง — latch รายวันใน `S.waterCelebDay` กลืนธงทิ้ง (เจ้าของเทสมาทั้งวัน
      ค่าถูกตั้งเป็นวันนี้ไปแล้ว) ⇒ ถอด latch ออก เพราะตัวจับเป็นขอบขาขึ้น + ธงถูกกินครั้งเดียว
      อยู่แล้ว = หนึ่งครั้งต่อการข้ามเป้าจริงโดยธรรมชาติ
   🔇 ซิงก์อัตโนมัติเบื้องหลังไม่ยกแถบ "กำลังซิงค์…" อีกแล้ว (แถบเป็นของสิ่งที่ผู้ใช้สั่งเอง) */
/* v1114 = ⌚ วัดสดครบเท่าที่รุ่นนี้ทำได้: **ชีพจร + ออกซิเจน**
   ตรวจตารางความสามารถของเครื่องจริงแล้ว — HRV กับความเครียด **สั่งวัดจากแอปไม่ได้**
   (`hrvAppDetectFunction = UNSUPPORT` · stress เป็นหน้าจอบนตัวนาฬิกาเท่านั้น)
   ⇒ ไม่ใส่ปุ่มหลอก · การ์ด HRV บอกตรง ๆ ว่านาฬิกาวัดเองระหว่างวัน
   ออกซิเจนมีแถบความคืบหน้า (ใช้เวลา 30-60 วิ ถ้าไม่โชว์คนจะกดหยุดก่อนได้ค่า)
   วัดทีละอย่าง — BLE ทำได้ทีละคำสั่ง · ออกจากหน้าแล้วหยุดเสมอ (กันกินแบตนาฬิกา) */
/* v1115 = ⌚ **เปิดวัดชีพจรอัตโนมัติ 24 ชม.** (ฝั่ง native — ต้องลง APK ใหม่)
   สวิตช์นี้ปิดมาจากโรงงานและเราไม่เคยสั่งเปิด ⇒ นาฬิกาวัดเฉพาะตอนถูกสั่งเท่านั้น
   ตอนนี้สั่งเปิดตอนเชื่อมทุกครั้ง ⇒ ชีพจรถูกบันทึกลงก้อน 5 นาทีเองตลอดวัน
   (`readDays` อ่านช่องนั้นอยู่แล้ว) ปุ่ม "วัดตอนนี้" เหลือไว้สำหรับเช็กเดี๋ยวนั้น
   ✏️ "นาทีขยับ" → "ระยะเวลาที่ขยับทั้งหมด (นาที)" — ชื่อเดิมเดาที่มาไม่ออก */
/* v1116 = 📖 บอกผู้ใช้ว่าข้อมูลย้อนหลังได้ถึงไหน
   · IUFIT Band — ท้ายหน้านาฬิกาบอกความจุในตัวเครื่อง (ตัวเลขมาจากนาฬิกาเอง ไม่ได้เดา)
     บันทึกได้แม้ปิดแอป/ไม่ได้พกมือถือ · เกินกำหนดวันเก่าสุดถูกทับ
   · Health Connect — อ่านย้อนได้ 30 วันนับจากวันที่กดอนุญาตครั้งแรก (กฎของ Android
     ต้องมีสิทธิ์ READ_HEALTH_DATA_HISTORY ถึงจะไกลกว่านั้น ซึ่งเราไม่ได้ขอ)
     ⚠️ ลบแอปแล้วลงใหม่ = วันเริ่มต้นถูกนับใหม่ทั้งหมด — เขียนไว้ในศูนย์ช่วยเหลือแล้ว */
/* v1117 = ⌚ เข้าหน้านาฬิกาแล้ววัดชีพจรให้เลย ไม่ต้องกด (เว็บล้วน ไม่ต้องลง APK)
   เจ้าของรายงานว่ายังต้องคอยกดวัด — คนละเรื่องกับวัดอัตโนมัติ 24 ชม. ที่เพิ่งเปิด:
     · อัตโนมัติ 24 ชม. = นาฬิกาเก็บลงตัวเองทุก 5 นาที ⇒ เห็นเป็นค่าเฉลี่ยรายวันหลังซิงก์
     · เลขวิ่งบนจอ    = ต้องสั่งนาฬิกาวัดสด ซึ่งเดิมผูกไว้กับปุ่มอย่างเดียว
   ⇒ เปิดหน้ามาสั่งวัดให้เลย · ยังหยุดเองใน 1 นาที และหยุดทันทีที่ออกจากหน้า */
/* v1118 = 🎴 การ์ดแชร์ 3 ใบใหม่จากดีไซน์ New3-Figma (ใช้ข้อมูลนาฬิกาล้วน)
   · ระยะทาง — เทียบเส้นทางจริงในไทย (กทม.→หัวหิน ฯลฯ · lib/distanceRoutes.ts)
   · คุณภาพการนอน — คะแนน + สัดส่วน 3 ระยะ + เวลาเข้านอน–ตื่น
   · เป้าสัปดาห์ — 3 วง: ก้าว · นาทีขยับ · การนอน
   ⚠️ ต้องลง APK ใหม่: เวลาเข้านอน–ตื่นเป็นข้อมูลใหม่จาก SleepData (getSleepDown/Up)
   + เป้ารายสัปดาห์เพิ่ม actMin/sleepMin (ของเดิมมีแค่ ก้าว/ระยะ/แคล/วัน)
   🐛 จับได้ระหว่างเขียนเทส: ตารางเส้นทางเรียงผิดหนึ่งแถว + ตัวเลือกใช้ break
      ⇒ เดิน 400 กม. ได้ "เชียงใหม่→เชียงราย" แทน "กทม.→โคราช" · แก้ทั้งสองจุดแล้ว */
/* v1119 = 🔤 **ฟอนต์ทั้งแอปตรงกันแล้ว** — `<button>/<input>/<select>/<textarea>`
   ไม่สืบทอดฟอนต์เองตามสเปกเบราว์เซอร์ มันใช้ฟอนต์ระบบของ OS แทน
   ⇒ ปุ่มทั่วแอปเป็นคนละฟอนต์กับข้อความรอบตัวมาตลอด (เห็นชัดที่ปุ่มภาษาไทย)
   ตรวจแล้วพบ 55 คลาสปุ่มใน 34 ไฟล์ที่ไม่ได้ตั้งฟอนต์เอง ⇒ แก้ด้วยกฎรวมกฎเดียว
   + verify:csstokens กันไม่ให้ถูกลบทิ้ง
   ✅ ติ๊กถูกในการ์ดแผนวันนี้ = วงกลมไล่สีเขียว + ติ๊กวาดเอง (คมทุกเครื่อง ไม่ขึ้นกับฟอนต์)
   ❤️ ชีพจรเป็นป้ายทรงเม็ดยา · หัวใจเต้นเฉพาะตอนกำลังวัดจริง
      + บอกชัดว่าเลขที่เห็นเป็นค่าเฉลี่ยของวัน ไม่ใช่ชีพจรปัจจุบัน
   🐛 หน้ารายละเอียดนาฬิกา: ปุ่มกลับใช้ไม่ได้เมื่อเข้าหน้านี้เป็นหน้าแรกของประวัติ
      (เปิดจากลิงก์ตรง · SW ปล่อยเวอร์ชันใหม่แล้ว reload · ผู้ใช้รีเฟรชเอง)
      ⇒ ไม่มีที่ให้กลับ = พาไปหน้านาฬิกาแทน */
/* v1120 = 👆 **ปัดลงเพื่อปิดชีตได้ทั้งแอป** (52 จุด ด้วยตัวติดตั้งตัวเดียว)
   ไม่ได้ปิดชีตเอง แต่แปลงการปัดลงเป็น "แตะพื้นหลัง" ⇒ เงื่อนไขการปิดของแต่ละใบ
   (ถามก่อนทิ้งงาน · ห้ามปิดระหว่างยินยอม · คืนช่องประวัติให้ปุ่มย้อนกลับ) ยังทำงานครบ
   ⚠️ รับเฉพาะท่าลงล่าง · ไม่รับจากขอบจอ 24px และไม่รับท่าแนวนอน
      — สองอย่างนั้นเป็นท่าย้อนกลับ/ออกจากแอปของ Android ห้ามไปซ้อน
   🧠 IU MATE สรุปร่างกายรายวันจากค่าที่นาฬิกาจับได้ (คะแนนความพร้อม 0-100
      เทียบกับฐานของเจ้าตัวเอง · ต้องใส่นาฬิกาครบ 4 วันก่อนจึงเริ่มให้คะแนน)
   ❤️ ชีพจรเป็นป้ายทรงเม็ดยา · หัวใจเต้นเฉพาะตอนกำลังวัดจริง */
/* v1121 = 🐛 **หน้านาฬิกาขึ้น "ยังไม่ได้เชื่อม" กับคนที่ใช้ Health Connect**
   หน้านี้ตัดสินจาก MAC ของ IUFIT Band อย่างเดียว ⇒ ผู้ใช้ Samsung/Garmin/Fitbit/Mi
   เห็นหน้าชวนเชื่อม ทั้งที่ข้อมูลเขาซิงก์เข้ามาแล้วและโชว์อยู่บนหน้าแรก
   + ซ่อนปุ่ม "วัดเดี๋ยวนี้" ของชีพจร/ออกซิเจน (Health Connect สั่งวัดไม่ได้ อ่านได้อย่างเดียว)
   + ไม่ขึ้นสถานะ "ติดต่อนาฬิกาไม่ได้" (ไม่มีลิงก์ BLE ให้หลุด)
   + ชื่ออุปกรณ์เขียนว่า Health Connect ไม่ใช่เดาว่าเป็น IUFIT Band
   + บอกขอบเขต 30 วันของ Health Connect (นับจากวันที่กดอนุญาต) */
/* v1122 = 🐛 **IU MATE สรุปร่างกาย: ชวนผิดจังหวะ + กดแล้วข้อความไม่มา**
   1. บับเบิลเดาเงื่อนไขเอง ("วันนี้มีข้อมูลไหม") ซึ่งหลวมกว่าเงื่อนไขจริงของตัวคำนวณ
      (ต้องเก็บฐานครบ 4 วันก่อนถึงให้คะแนนได้) ⇒ ชวนกดทั้งที่เปิดไปเจอแค่ "กำลังเก็บฐาน"
      ตอนนี้ถามตัวคำนวณตรง ๆ ผ่าน `dailyBodyNow()` — กฎอยู่ที่เดียว
   2. `daily_body` ตกหล่นจาก EPHEMERAL_INTENTS ⇒ การ์ดถูกเก็บลงประวัติ ⇒ รอบถัดไป
      ตัวกันซ้ำเจอใบเก่าแล้ว return เงียบ ⇒ "แชทเปิดขึ้นมาแต่ข้อความไม่มา"
   3. กดซ้ำระหว่างวันตอนนี้ **แทนที่** ใบเก่าด้วยค่าสด ไม่ใช่ข้ามไปเฉย ๆ */
/* v1123 = 🐛 **กดวัดออกซิเจนแล้วไม่ได้ค่าเลย** — เจอ 3 ชั้นซ้อนกัน
   1. ปลั๊กอินตอบกลับเว็บ **ในคอลแบ็กแรกเท่านั้น** ⇒ นาฬิกาไม่ตอบ = `await` ค้าง
      ตลอดกาล · `oxOn` ไม่เคยเป็น true ⇒ กดปุ่มแล้วหน้าจอไม่ขยับเลย (บั๊กเดียวกับชีพจร)
   2. เราทิ้ง `Spo2hData.spState` / `.deviceState` ที่นาฬิกาส่งเหตุผลมาให้ทุกครั้ง
      ⇒ วัดไม่ได้แล้วไม่มีใครรู้ว่าเพราะอะไร · ตอนนี้บอกได้: ใส่ไม่แนบ · เซ็นเซอร์ไม่ว่าง
      · ชาร์จอยู่ · แบตต่ำ · ฟังก์ชันปิดอยู่ที่ตัวนาฬิกา
   3. หน้านี้สั่งวัดชีพจรอัตโนมัติตอนเปิด ⇒ กดออกซิเจนทันทีชนกันเอง
      (เซ็นเซอร์ PPG ตัวเดียวทำได้ทีละอย่าง) · หยุดชีพจรแล้วหน่วง 0.9 วิ ก่อนสั่งต่อ
   ⚠️ ต้อง build APK ใหม่ด้วย — ข้อ 1-2 อยู่ฝั่ง native */
/* v1124 = 🛡️ **ออดิตข้อมูลนาฬิกาทั้งเส้น แล้วแก้ยกชุด** (ทั้ง IUFIT Band และ Health Connect)

   🔴 ตัวร้ายที่สุด — `wearIngest()` เขียน 0 ทับข้อมูลจริงที่เคยมี
      การ์ดกัน `!= null` ถูกออกแบบให้ `undefined` = ไม่ทับ แต่ไม่มีผู้เรียกรายไหน
      ส่ง `undefined` เลย (ทั้งสองฝั่งบังคับเป็น 0 ก่อนส่ง) ⇒ 0 ชนะค่าจริง
      เกิดเองได้: ปิดสิทธิ์อ่านก้าวใน Health Connect แต่ยังเปิดการนอน
      → ก้าวจริง 7 วันย้อนหลังถูกทับเป็น 0 ถาวร ติดไป cloud backup ด้วย
      ตอนนี้มีสัญญากลาง `WearDay v2` + `mergeWearDay()` + 18 เคสทดสอบตรึงไว้

   🔴 `hr`/`sleep` merge ทั้งก้อน ⇒ Health Connect ทับข้อมูลแบนด์ทิ้ง
      (ชีพจรขณะพัก + รายละเอียดการนอนหายทั้งชุด) · ตอนนี้ merge รายฟิลด์ย่อย

   🔴 การนอนจาก Health Connect เป็น "เวลาอยู่บนเตียง" ไม่ใช่ "เวลาหลับ"
      ตัวกรองช่วงตื่นเป็น no-op เพราะปลั๊กอินไม่เคยส่งฟิลด์ที่เราเช็ก
      ⇒ คืนที่ตื่นบ่อยกลายเป็น "นอนมากขึ้น" · ตอนนี้ใช้ `stages[]` ที่ปลั๊กอินส่งมาให้ฟรี
      ได้หลับลึก/หลับตื้น/จำนวนครั้งที่ตื่น ของผู้ใช้ Health Connect เป็นครั้งแรกด้วย

   🔴 `readDays()` ไม่มีเพดานเวลา ⇒ callback ไม่มา = `wearSyncing` ค้าง true ถาวร
      = ซิงก์อัตโนมัติตายจนปิดเปิดแอป · ใส่เพดาน 90 วิ + watchdog ฝั่ง JS อีกชั้น

   🟠 อุด Promise ค้างอีก 6 จุด · เปิดสวิตช์วัดออกซิเจนกลางคืนตอนเชื่อม
   🟠 สิทธิ์ Health Connect บางส่วน = เคยนับว่าสำเร็จเงียบ ๆ ตอนนี้บอกผู้ใช้ + มีปุ่มซิงก์เอง
   🟠 เลิกสัญญาสิ่งที่ผู้ใช้ Health Connect ไม่มีวันได้ (SpO2/HRV/ชีพจรพัก)
   🟠 เลิกโชว์ "0" / "นอน 0:00" เมื่อไม่มีข้อมูล · `migrateWear` เลิกทำ SpO2/HRV หาย
   🟡 ลบ health permission ที่ประกาศแต่ไม่ใช้ (เสี่ยง Play ตีกลับ)

   ⚠️ ต้อง build APK ใหม่ด้วย — ครึ่งหนึ่งอยู่ฝั่ง native */
/* v1125 = 🔴🔴 **ก้าวที่โชว์ต่ำกว่าจริง ~87%** — ปิดจบด้วยหลักฐานจากเครื่องจริง
   log จาก SM-S928B (2569-07-26) ชี้ขาดสองข้อที่ค้างมาตลอด:

     readDays day=2026-07-26 rawDis=0.422 stepsMax=499 stepsSum=3976
     readDays day=2026-07-25 rawDis=0.022 stepsMax=26  stepsSum=26

   1. `stepValue` เป็น **ค่าราย 5 นาที ไม่ใช่ค่าสะสม** ⇒ ต้องบวก ไม่ใช่เอามากสุด
      แอปเคยโชว์ 499 ก้าว ทั้งที่เดินจริง 3,976 ก้าว · แคลอรี่ผิดแบบเดียวกัน
      (`actMin += 5` ที่ออดิตเคยทักว่าขัดกัน — ถูกมาตลอด คนที่ผิดคือ `maxOf`)
   2. `disValue` หน่วยเป็น **กิโลเมตร** ไม่ใช่เมตรตามป้ายเดิม
      ⇒ ลบ heuristic เดาหน่วยจากจำนวนก้าวทิ้ง เหลือสูตรเดียว
   ⚠️ ต้อง build APK ใหม่ — ทั้งสองข้ออยู่ฝั่ง native */
/* v1126 = 📅 **หน้านาฬิกาตามวันที่ที่เลือกจากหน้าแรกแล้ว**
   ทุกหน้าที่แสดงข้อมูลรายวัน (โฮม · น้ำ · อาหาร · ร่างกาย) ใช้ตัวชี้วันกลาง
   `daily.selectedDate` ร่วมกันอยู่แล้ว มีแค่หน้านาฬิกาสองหน้าที่ยังยึด "วันนี้จริง"
   ⇒ เลื่อนไปดูเมื่อวานที่หน้าแรก แล้วเข้าหน้านาฬิกา ได้ข้อมูลวันนี้แทนเงียบ ๆ
   + แถวเลื่อนวัน ‹ › + "กลับมาวันนี้" บนหน้านาฬิกา (เลื่อนที่ไหนก็ตามกันทั้งแอป)
   + กราฟ 7 วันจบที่วันที่กำลังดู ไม่ใช่จบที่วันนี้เสมอ
   ⚠️ สิ่งที่ **ไม่** ตามวัน เพราะเป็นสถานะของตัวเครื่องไม่ใช่ของวัน:
      แบต · เวลาซิงก์ · ความจุหน่วยความจำ · **การวัดสด** (ซ่อนปุ่มเมื่อดูวันย้อนหลัง
      เพราะค่าที่วัดคือชีพจร "เดี๋ยวนี้" เอาไปโชว์คู่ข้อมูลเมื่อวานคือปนสองเวลา) */
/* v1127 = 📤 **ปุ่มแชร์การ์ดใช้ได้จริงในแอปแล้ว** + ตัวเลขในการ์ดหลุดกรอบ
   🐛 ในแอปเส้นทางแชร์ตายทั้งสองทาง: Web Share API ไม่เปิดใน Android WebView
      และ `<a download>` ไม่มีตัวจัดการดาวน์โหลด · แถม `a.click()` ไม่เคยโยน error
      ⇒ โค้ดสรุปว่าสำเร็จ ขึ้น "บันทึกรูปแล้ว" ทั้งที่ **ไม่มีไฟล์เกิดขึ้นเลย**
      ⇒ ปลั๊กอินใหม่ `IufitShare`: แชร์ผ่านแผงของ Android (IG/LINE/FB) + บันทึกลง
        **คลังภาพจริง** ผ่าน MediaStore (`@capacitor/filesystem` ลงคลังภาพไม่ได้)
   🐛 ตัวเลขในวงแหวนการ์ด "เป้าหมายสัปดาห์" ทะลุกรอบ — `font-size` ที่อยู่ในกรอบ
      `viewBox` เป็น user unit ไม่ใช่พิกเซล แต่ถูกคูณสเกลการ์ด 3.33× ซ้ำอีกชั้น
      (แก้ 2 การ์ด: วงแหวนเป้าหมาย 56.7→17 · ป้ายหมุดการ์ดระยะทาง 36.7→11)
   ⚠️ ต้อง build APK ใหม่ — ปลั๊กอินอยู่ฝั่ง native */
/* v1128 = 🃏 **การ์ดแชร์ชุดใหม่ตาม handoff (สร้างไว้ก่อน ยังไม่ต่อเข้าแอป)**
   สเปกสั่งว่าการ์ดทั้ง 7 ใบใช้โครงเดียวกัน "ไม่มีธีมให้เลือก เพื่อให้จำแบรนด์ได้"
   ⇒ ทำเป็นคอมโพเนนต์เดียวขับด้วยพร็อพ (`ShareTemplateCard.vue`) ไม่ใช่ 7 ไฟล์แยก
      ระบบเดิมแยก 8 ไฟล์แล้วสไตล์เพี้ยนออกจากกัน (เพิ่งเจอบั๊กฟอนต์ SVG พร้อมกัน 2 ใบ)
   + `lib/shareTemplates.ts` — รายการ 7 เทมเพลต + กฎ "ไม่มีข้อมูลให้ซ่อน ไม่ใช่โชว์ว่าง"
     + ปัดวนสองทาง · 13 เคสทดสอบ
   + ส่งออกเป็น JPG ได้แล้ว (สเปกระบุ JPG · พื้นทึบเสมอ ไฟล์เล็กกว่า PNG มากที่ 1080×1920)
     ปลั๊กอิน native เลือก MIME ตามนามสกุลจริง — ตั้งผิดแล้ว IG ปฏิเสธไฟล์
   ⚠️ โลโก้ใช้ wordmark ตัวจริงของแอป (Montserrat 800 เอียง + skew 4° + FIT สีแบรนด์)
      และฟอนต์ยึด `--font-ui` ของแอป ไม่ใช่ `Prompt` ตามม็อกอัพ — แอปไม่ได้โหลด Prompt
      ถ้าเขียนตามม็อกอัพจะตกไปใช้ฟอนต์ระบบ = ไม่เหมือนทั้งแอปและไม่เหมือนม็อกอัพ */
/* v1129 = 🔗 **เชื่อมสองระบบตาม handoff — คอมโพสเซอร์เป็นเจ้าของงานแชร์ชิ้นเดียว**

   handoff แบ่งหน้าที่ไว้และเขียนกำกับเองว่า "สำคัญ — อย่าสลับ":
     overlay ฉลอง = แอปเด้งให้เอง · มี IU Mate · เจ้าตัวดูคนเดียว · **ไม่ส่งออกไฟล์**
     คอมโพสเซอร์  = ผู้ใช้กดแชร์ · ไม่มี IU Mate · คนนอกเห็น · ส่งออก JPG 1080×1920

   · overlay เลิกส่งออกไฟล์ ⇒ ปุ่มหลักเปิดคอมโพสเซอร์พร้อมเลือกเทมเพลตที่ตรงกับ
     เหตุการณ์ไว้แล้ว (แมป 8 เหตุการณ์ → 7 เทมเพลต · สองใบยุบรวมโดยตั้งใจ)
   · ทางเข้าที่ 1 หน้าผลลัพธ์ → เปิดที่ "ความคืบหน้าน้ำหนัก" (ใช้ได้ทุกคน)
   · ทางเข้าที่ 2 หน้านาฬิกา → ปุ่มข้างชิปแบต เปิดที่ "คะแนนร่างกาย"
   · `ShareCardSheet` ตัวเก่าถูกแทนที่ — เดิมมีการ์ดแชร์ซ้ำสองระบบ ตัดเหลือระบบเดียว
   · ปัดซ้าย-ขวาเปลี่ยนเทมเพลต (threshold 40px) + ล็อกแกนกันชนกับ "ปัดลงปิดชีต"

   ⚠️ คีย์ข้อความอยู่ก้อน **แกน** ไม่ใช่ก้อน lazy — overlay เป็น global เปิดคอมโพสเซอร์
      ได้จากทุกหน้า ถ้าอยู่ก้อน lazy ผู้ใช้ที่ฉลองอยู่หน้าอาหารจะเห็นคีย์ดิบ
      (`verify:i18n` จับให้เองตอนทำ — ด่าน LAZY_ROUTE_KEY) */
/* v1130 = ⌚ **หน้าตานาฬิกาทั้งชุดตาม handoff** (หมวด B + C) + รื้อสูตรคะแนนร่างกาย

   🧮 สูตรใหม่ = **การนอน 40 + การฟื้นตัว 35 + การขยับ 25** (handoff · ตรงกับหลักสากล
      Whoop = HRV เด่น + ชีพจรพัก + นอน · Oura = นอน + ชีพจรพัก + HRV + activity balance
      · Fitbit = นอน + ภาระกิจกรรม + HRV) · เส้นแบ่ง 70 / 40 · ฐาน **14 วัน**
      ⭐ ภาระเมื่อวานเป็น **ตัวหัก ไม่ใช่ตัวบวก** — ใกล้ฐานตัวเองได้สูงสุด ทั้งน้อยเกิน
         และหนักเกินหักคะแนน (Oura activity balance) · "เดินเยอะ = พร้อมซ้อมหนัก" ผิดสรีรวิทยา
      ⚠️ ผู้เรียกเดิมป้อนหน้าต่างแค่ 7 วัน ⇒ คะแนนจะเป็น null ตลอดกาลโดยไม่มี error
         ขยายเป็น 21 วันทั้งสองจุดแล้ว (`stores/iumate.ts` · `useShareTemplateData.ts`)

   📊 หน้านาฬิกา 4 สถานะ (ปกติ / กำลังซิงค์ / หลุดการเชื่อมต่อ / ยังไม่มีข้อมูลวันนี้)
      + การ์ดคะแนนพื้นเข้มพร้อมคะแนนย่อย 3 ก้อน + การ์ดก้าว + กริดเมตริก 5 ใบ
   📄 หน้ารายละเอียด 7 หน้า (เพิ่ม `body` = ที่มาของคะแนน) พร้อม disclaimer บังคับ

   🩺 HRV: พบว่าข้อมูลจริงของรุ่น proto=5 มาทางสตรีม DF ที่เราทิ้งทุกรอบ
      (คำสั่ง `readHRVOrigin` เดินโปรโตคอลเก่าซึ่งรุ่นนี้ตอบแพ็กเก็ตเปล่า → `kept=0`)
      ⇒ เก็บสตรีมนั้นแล้ว · SpO2 กลางคืนเป็นเรื่องเดียวกัน แก้พร้อมกัน

   ⚠️ handoff วาดจากข้อมูลสมมติ ของจริงเรามีน้อยกว่า — บล็อกที่ไม่มีข้อมูลถูก **ซ่อน**
      ไม่ใช่กุตัวเลข (ก้าวรายชั่วโมง · cadence · โซนหัวใจรายเวลา · SpO2 รายชั่วโมง ·
      สเกลความเครียด ซึ่งนาฬิกาไม่ได้ให้ค่าความเครียดมาเลย)
   ⚠️ ต้อง build APK ใหม่ — งาน HRV อยู่ฝั่ง native */
/* v1142 = ⌚ **แก้ "เชื่อมนาฬิกาเรือนเดิมไม่ได้"** + เมนูจัดการอุปกรณ์ที่จับคู่
   หลักฐานจากเครื่องจริง: `connect: start mac=… name=''` แล้วเงียบ 20 วิจนหมดเวลา
   `connectDevice(mac, name, …)` ของ Veepoo ได้ชื่อว่าง = **ไม่ callback กลับมาเลย**
   เดิมเก็บแค่ MAC ⇒ จับคู่ครั้งแรกติด (ได้ชื่อจากการสแกน) แต่เชื่อมกลับส่งชื่อว่างเสมอ
   ⇒ auto-reconnect ไม่เคยทำงานเลยตั้งแต่ต้น ⇒ ชีพจร/SpO2/ซิงก์เงียบตามกันหมด
   + `ensureLink()` มีตัวกันเรียกซ้อน (เจอ connect ซ้อนกันห่าง 1.9 วิในล็อกเดียวกัน)
   + เมนู "แก้ไขอุปกรณ์ที่จับคู่": เปลี่ยนชื่อ · จับคู่เรือนอื่น · ยกเลิกการจับคู่

   ⚠️ **ขยับเลขทุกครั้งที่ deploy** — ไม่ใช่เฉพาะตอนแก้ตัว SW
   เลขนี้เป็นทางเดียวที่ตรวจจากนอกเครื่องได้ว่าโค้ดชุดไหนขึ้นไปแล้ว
   (ก้อน /assets มี hash ในชื่อ ตรวจด้วยตาไม่ได้) */
/* v1143 = 🐛 "ค้นหาไม่เจอเรือนที่ใช้อยู่ แต่เจอเรือนที่ไม่ได้ใช้"
   อุปกรณ์ BLE **หยุดกระจายสัญญาณเมื่อถูกเชื่อมต่ออยู่** ⇒ เรือนที่มีใครถือลิงก์ไว้
   จะหายจากผลสแกนของทุกแอป · คนถืออาจเป็นแอปผู้ผลิต หรือ **ตัวเราเอง**
   (ซิงก์อัตโนมัติเชื่อมกลับทุก 2 นาที — ยิ่งถี่ยิ่งชนกับจังหวะที่ผู้ใช้กดค้นหา)
   ⇒ ตัดการเชื่อมต่อ + รอ 600ms ก่อนสแกนเสมอ · และซิงก์อัตโนมัติเว้นวิทยุให้ระหว่างสแกน */
/* v1144 = ⌚ **เชื่อมค้างตลอดจนกว่าผู้ใช้จะสั่งตัด** (เจ้าของสั่ง: แบบ Whoop)
   ต่อสายฝั่งเว็บเข้ากับ `WearLinkService` (FGS ชนิด connectedDevice ฝั่ง native)
   · จับคู่/เชื่อมกลับสำเร็จ → `startLink` · ตัดการเชื่อมต่อ/เลิกจับคู่ → `stopLink`
   🐛 และ **ปิดรอบซิงก์ 2 นาทีเมื่อ service ถือลิงก์อยู่**
      อาการที่เจอทันทีตอนเปิด service: "หลุด ๆ ติด ๆ · วัด bpm ไม่ขึ้น"
      เพราะสองระบบแย่งวิทยุกัน — ทั้งคู่ถูกในตัวเอง แต่ทำงานพร้อมกันแล้วพังกว่ามีตัวเดียว
   ⚠️ ต้องลง APK ใหม่ด้วย — service อยู่ฝั่ง native */
/* v1145 (2569-07-29) — "กำลังซิงค์เด้งเอง + หลุด ๆ ติด ๆ"
   · แถบ "กำลังซิงค์…" เป็นของการกระทำที่ผู้ใช้กดเองเท่านั้นแล้ว:
     เปิดหน้านาฬิกา / ซิงก์เบื้องหลังของ Health Connect / ก้อนแรกหลังจับคู่ = เงียบหมด
     (healthSyncNow ได้พารามิเตอร์ quiet · pullNow ตอน mount ส่ง quiet)
   · สแกน = หยุด WearLinkService ก่อนเสมอ — ไม่งั้นมันต่อกลับใส่เรือนที่กำลังหา
   · service ถือลิงก์อยู่ → รอบซิงก์กลับมาอ่านข้อมูลแบบเงียบ **เฉพาะตอนลิงก์ติด**
     (เดิมหยุดสนิท = ข้อมูลไม่ไหลเลย) และถามสถานะ service ใหม่ทุกครั้งที่กลับเข้าแอป
   ⚠️ ตัวกันเชื่อมซ้อนฝั่ง native (VeepooAdapter) ต้องลง APK ใหม่ถึงจะได้ */
/* v1146 = 🐛 **เชื่อมนาฬิกาอยู่แล้วเข้าเมนูจัดการอุปกรณ์ไม่ได้เลย**
   เจ้าของเจอตอนทดสอบ: แถวนาฬิกาในตั้งค่าเปลี่ยนเป็น "แตะเพื่อตัดการเชื่อมต่อ"
   เมื่อเชื่อมอยู่ ⇒ ชีตที่เปิดได้มีแค่ "ตัดการเชื่อมต่อ" ⇒ จะเปลี่ยนชื่อหรือ
   จับคู่เรือนอื่นต้อง **ตัดการเชื่อมต่อทิ้งก่อน** ทั้งที่ไม่ได้อยากตัด
   ⇒ เพิ่มปุ่ม "แก้ไขอุปกรณ์ที่จับคู่" ในชีตตัดการเชื่อมต่อ (ทางเดียวที่เข้าได้ตอนเชื่อมอยู่) */
/* 🔴 2569-07-30 · บทเรียนที่เพิ่งเจอของจริง — **ชื่อแคชใหม่ ≠ ของข้างในใหม่**
   build 16:38 กับ build 17:10 ใช้ชื่อแคชเดียวกัน (`v1158`) เพราะลืมขยับระหว่างสอง build
   ⇒ ไบต์ของ sw.js ไม่เปลี่ยน ⇒ `install` ไม่ทำงาน ⇒ `precacheShell()` ไม่ถูกเรียกซ้ำ
   ⇒ แคชยังถือ `index.html` ของ 16:38 ซึ่งอ้าง hash ของก้อน asset ชุดเก่า
   ⇒ เบราว์เซอร์ขอก้อนเก่า เจอในแคช จบ · **งานที่แก้ไปไม่ถึงผู้ใช้เลย แม้รีโหลดหลายครั้ง**

   ที่ร้ายคือชื่อแคชดู "ใหม่" อยู่แล้ว จึงไม่มีใครสงสัย — ต้องล้าง SW ทิ้งจึงเห็นของจริง
   ⇒ ยืนยันแล้วว่ากฎ "ขยับเลขทุกครั้ง" **พึ่งความจำคนไม่ได้**
      ทางแก้ที่ควรทำ: ให้ขั้นตอนหลัง build ประทับเลขจาก hash ของบิลด์ลง sw.js อัตโนมัติ
      (ที่เดียวกับ `scripts/make-404.mjs`) แล้วเลิกขยับมือ — ดู `ทำต่อ-BrandCI-2569-07-30.md` */
/* v1179 · รอบ "เก็บบั๊กที่เห็นกับตาบนเบราว์เซอร์จริง":
   · 🔴 `hrDayAvg` — ชีพจรเฉลี่ยทั้งวันมีช่วงของตัวเอง (25-180) แยกจาก `hr` (25-240
     ซึ่งเป็นช่วงของตัวอย่างรายก้อนตอนออกกำลัง) ⇒ `hr.avg = 210` เลิกขึ้นจอ
     และเลิกดันค่าเฉลี่ย 7 วันเป็น 99 BPM
   · 🔴 `/backup` — latch `iufit_corrupt_recovered` · กล่องเปลี่ยนเป็นโทนข้อมูลหลังกู้
     กดซ้ำได้แต่ต้องยืนยันพร้อมบอกว่าจะทับอะไร · ต้นฉบับยังถูกเก็บไว้ครบ
   · 🟠 `/watch/*` — ช่องว่างมีคำอธิบายเดียว (เลิกขึ้น "ยังไม่มีข้อมูล" คู่กับ "ค่านอกช่วง")
   · 🟠 `/line-log` — ปุ่มดึงข้อมูล/ยกเลิกการเชื่อม ปิดเมื่อรู้แน่ว่ายังไม่ผูก + บอกเหตุผล
   · 🟡 `/watch` — คำแนะนำเลิกเป็นตัวหนังสือสีแดงที่อ่านเหมือน error
   · 🟡 `tgtCautionYouth` เลิกชี้ไปที่ "ตัวเลขที่แสดง" ที่หน้า /food ไม่มี
   · 🟡 หน้าแรกที่ 320px — วงแหวนกับสารอาหารเลิกเบียดกันจนเป้าโปรตีน/ไขมัน/คาร์บถูกตัดหาย */
/* ── รอบ 2569-08-03 ────────────────────────────────────────────────────────────
   · 🔴 i18n · เปิดเว็บตรงเข้า `/profile` ในโหมด EN แบบ hard reload แล้วก้อน `onb`
     ทั้งก้อนตกกลับเป็นภาษาไทย (act1–act5 · "ไม่ระบุ" · คำถามตั้งครรภ์/ให้นมบุตร)
     — `app.use(router)` เริ่ม navigation แรกก่อน `setLocale()` จะ apply ⇒ `loadI18n()`
     อ่าน locale ได้ 'th' แล้วลงทะเบียนก้อนฝั่งไทยก้อนเดียว · ดู `targetLocale`
     ใน src/i18n/index.ts + ด่าน BOOT_RACE ใน scripts/verify-i18n.mjs
   · 🟢 onboarding · คำถามตั้งครรภ์/ให้นมบุตร (ก้อน `onb`) ได้ภาษาอังกฤษครบแล้ว
   · 📄 เอกสาร deploy/runbook เลิกชี้ไฟล์ผี + ด่านใหม่ `verify:docrefs` */
/* v1181 · รอบตรวจรับปิดท้าย — **"ด่านสองตัวที่แดงไม่ได้"**
   ─────────────────────────────────────────────────────────────────────────────
   รอบนี้ไม่ได้แก้พฤติกรรมแอปเลยแม้แต่บรรทัดเดียว (`src/` เปลี่ยนแค่คอมเมนต์)
   ⇒ **ก้อน bundle เหมือน v1180 ทุกไบต์** · ขยับเลขไว้เพื่อให้เครื่องที่ติดตั้งแล้ว
   มีจุดอ้างอิงตรงกับบันทึกรอบนี้ ไม่ใช่เพราะมีโค้ดใหม่ต้องส่ง

   · 🔴 `verify:i18n` หัวข้อ F (BOOT_RACE) **เขียวปลอม — ล้มไม่ได้เลย**
     ถอด `targetLocale` ออก · ถอดลูปใน `setLocale()` ออก · ถอดทั้งคู่ (= สภาพก่อนแก้)
     ⇒ เขียวทั้งสามแบบ · ต้นเหตุ: ฉากเดิมหน่วง navigation แค่ `await Promise.resolve()`
     ซึ่ง `wanted.add()` ทันสแนปช็อตเสมอ ⇒ หน้าต่างที่ก้อนจะตกหล่นไม่เคยเปิด
     ⇒ เขียนใหม่: ฉากละโปรเซส · F1 กวาด **ทุก route ในตาราง (42 เส้นทาง · 30 ก้อน)**
       แล้ววัดคุณสมบัติที่สังเกตได้จริง — "ต้นไม้ภาษาไทยห้ามโตขึ้นเลยตลอดเซสชัน EN"
       (ถอด `targetLocale` ⇒ แดง 2,573 คีย์) · F4 คุม `pendingSwitch` (ถอด ⇒ แดง)
     ⚠️ ลูป `for(;;)` ใน `setLocale()` พิสูจน์แบบนั้นไม่ได้ — มันซ้ำซ้อนกับ `targetLocale`
        โดยสมบูรณ์ · เก็บไว้เป็นชั้นสอง แต่ห้ามอ้างว่า F2/F3 เขียวแปลว่ามันยังทำงาน
   · 🔴 ชุด golden **แขวนตลอดกาล ⇒ `npm run verify:all` ไม่เคยพิมพ์ SUMMARY เลย**
     ตั้งแต่วันที่ลงทะเบียน golden เข้าเกตกลาง (2569-08-02) · ต้นเหตุ:
     `tests/waterCelebrate.golden.test.ts` ผ่านครบ 9 เคสแล้ว **ไม่จบโปรเซส** เพราะแตะ
     `useDailyStore()` ที่ตั้ง `window.setInterval(syncTodayCursor, 30_000)` ไว้ถาวร
     (ตัวเฝ้าเที่ยงคืน — ของแอปถูกแล้ว ที่ผิดคือตัวรันเทสที่ไปรอมัน)
     ⇒ ปิดสองชั้นใน `scripts/vitest-shim.mjs`: ลูก `process.exit(0)` เองหลังสรุปผล
       + พ่อใส่ `timeout` ให้ `spawnSync` ⇒ เทสที่ค้างกลายเป็นแดงที่อ่านออก ไม่ใช่เกตที่แขวน
     ⇒ ตอนนี้ golden จบใน ~15 วิ · 21 ไฟล์ · 439 เคส · ตก 0
   · ✅ รัน `verify:all` ครบทั้ง 39 ด่านเป็นครั้งแรกหลังงานตั้งครรภ์/ให้นมบุตร */
/* v1182 · รอบปิดรอบสองสาย — **"คอมเมนต์ไม่ใช่โค้ด"**
   ─────────────────────────────────────────────────────────────────────────────
   รอบนี้ **ไม่ได้แก้พฤติกรรมแอปเลยแม้แต่บรรทัดเดียว** (`src/` ไม่ถูกแตะถาวรสักไบต์ —
   มีแต่การแก้ตัวด่านใน `verify.*` / `scripts/verify-*`) ⇒ **ก้อน bundle เหมือน v1181
   ทุกไบต์** · ขยับเลขเพื่อให้เครื่องที่ติดตั้งแล้วมีจุดอ้างอิงตรงกับบันทึกรอบนี้

   · 🔴 **ยุบบั๊ก "ด่านอ่านร้อยแก้วเป็นโค้ด" ไว้ที่เดียว** — `scripts/code-only.mjs`
     รอบก่อนพบสองตัวที่เป็นหัวกับก้อยของเหรียญเดียวกัน (`verify.datasafety §17`
     แดงปลอมเพราะจับคอมเมนต์ "⛔ ห้ามเติม perinatal ที่นี่" · `verify.coachtarget §4`
     เขียวปลอมเพราะ `coachVisibleCautions(` โผล่ในคอมเมนต์ ⇒ ถอดตัวกรอง PDPA
     ตัวจริงออกแล้วยังเขียว) · รอบนี้ **ออดิตทั้งชุด `verify:*` แล้วแปลงจาก 2 → 24 ไฟล์**
     ⇒ ทุกไฟล์ที่แปลงต้องเรียก `assertCodeOnlySelfTest(ok)` ของตัวเอง
       (ด่านที่ตัดคอมเมนต์ ต้องพิสูจน์ได้ว่ามันไม่ได้เขียวเพราะมันตาบอด)

   · 🔴 **เขียวปลอมของจริงที่จับได้รอบนี้ 1 ตัว** — `verify-auth §17 selectorExists()`
     ต่อ `.vue` ทั้ง `src/` เป็นก้อนเดียวแล้วหา `data-tour="…"` แบบดิบ ๆ ·
     `CoachChatHubView.vue:6` และ `stores/tour.ts:137` มี `data-tour="coach-chat"`
     อยู่ใน **คอมเมนต์** ⇒ ลบแอตทริบิวต์ตัวจริงใน `CoachView.vue:285` ทิ้ง ด่านก็ยังเขียว
     (ทัวร์จะชี้ไปที่ว่างเปล่าโดยไม่มีใครร้อง) · พิสูจน์แล้ว: ก่อนแก้ = PASS · หลังแก้ = RED

   · 🔴 **`verify-css-tokens` ตาบอดกับบั๊กที่ตัวเองถูกเขียนขึ้นมาจับ** — `declaredIn()`
     กับ `selfProvided()` อ่าน `.css`/`.vue` ดิบ ⇒ โทเคนที่ประกาศอยู่ใน **คอมเมนต์บล็อก**
     ถูกนับว่า "ประกาศแล้ว" ⇒ คอมโพเนนต์ที่ใช้ `var(--x)` ผ่านฉลุย ทั้งที่เบราว์เซอร์
     ทิ้งพร็อพเพอร์ตี้นั้นเงียบ ๆ = **ปุ่มขาวบนขาว** · พิสูจน์ด้วยการฝังเคสจริงแล้วแดง

   · ⭐ ขยายตัวช่วยสองเรื่องที่รุ่นแรกครอบไม่ถึง:
       A. คอมเมนต์บล็อกของ CSS ใน `<style>` ของ `.vue`/`.html` และไฟล์ `.css`
          (ก่อนหน้านี้รอดทั้งก้อน ⇒ ด่าน `.iu-modal {` / hex ดิบ / ตัวเลขการ์ดแชร์
           อ่านคอมเมนต์เป็นกฎจริง)
       B. `{ keepOffsets: true }` — แทนคอมเมนต์ด้วยช่องว่างโดยเก็บ `\n` ครบ
          ⇒ ด่านที่พิมพ์ `ไฟล์:บรรทัด` และด่านที่เทียบ `indexOf(a) < indexOf(b)`
          แปลงได้โดยเลขไม่เพี้ยน (เจอของจริง: `verify-persistence` รายงานเลขบรรทัด
          เพี้ยนมาตลอดเพราะตัวตัดเดิมยุบความยาว)
     🐛 และปิดรู "บล็อกผี" ของตัวช่วยเอง: คอมเมนต์ HTML ที่ **เอ่ยถึง** `<style>`
        (`FoodAdjustSheet.vue` เขียนว่า "ดูบล็อก `<style>` ข้างล่าง") เคยเปิดบล็อกปลอม
        แล้วไปปิดที่ `</style>` ตัวจริง ⇒ คอมเมนต์ก้อนนั้นไม่ถูกตัด

   · ⛔ **ด่านที่ตั้งใจตรวจคอมเมนต์ ถูกกันไว้ไม่ให้แปลง 8 จุด** (มีโน้ต `⛔ ห้ามแปลง`
     กำกับที่ตัวโค้ด) — `verify-docrefs` ทั้งฉบับ · `verify-contrast` ทั้งฉบับ ·
     `healthmath §22.6` (ยืนยันว่ามีคำอธิบายกำกับใน `vitalsRange.ts`) ·
     `verify-pwa` §VAPID public key · `verify-pwa`/`verify-marketplace` §เหตุผลราคาคอร์ส PT ·
     `verify-no-lookup-route` §`@never-deployed` · `verify-templates` §"ยังไม่พอร์ต"
     (ยืนยันว่า**คอมเมนต์เก่าถูกลบแล้ว**) · `verify-auth §20` §`keep S.emailId`
     ⇒ แปลงพวกนี้ = ด่าน "เขียวเพราะตาบอด" ซึ่งแย่กว่าไม่มีด่าน

   · ✅ เกตปิดรอบ: `verify:all` **40/40 ด่าน · ตก 0** · `vue-tsc --noEmit` สะอาด ·
     `verify:i18n` th 3,431 = en 3,431 · ทั้ง 10 หัวข้อเป็น 0 ·
     `verify:contrast --gate` ผ่าน (self-test 18/18 · [T] 191/191 · [G] 149/149 · [U] 0/0) ·
     golden 21 ไฟล์ · 439 เคส · ตก 0 */
/* v1183 · 🔴 **รอบยิงด่วนวันกฎขึ้นจริง (2569-08-03)** — "เว็บต้องพูดภาษาเดียวกับกฎ"
   ─────────────────────────────────────────────────────────────────────────────
   ต่างจาก v1181/v1182 ที่ bundle ไม่เปลี่ยนเลย — **รอบนี้ `src/` เปลี่ยนจริง** และ
   ที่สำคัญกว่านั้น: กฎ RTDB ชุด PhaseD-v4-GOLIVE **ขึ้นโปรดักชันไปแล้ว** ขณะที่
   เครื่องผู้ใช้ยังกินบันเดิลเก่าอยู่ ⇒ เขียนค่าในรูปที่กฎใหม่ปฏิเสธ = ผู้ใช้พังคาที่
   ⇒ **ไม่ขยับเลขนี้ = deploy แล้วผู้ใช้เดิมยังพังเหมือนเดิม** เพราะ SW เก่าเสิร์ฟ
     `index.html` ที่อ้าง hash ของก้อน asset ชุดเก่า (ดูบทเรียน v1158 ด้านบน)

   งานที่ลงในรอบนี้:
   · 🟢 UI · ป้ายยืนยันอีเมล 3 สถานะที่การ์ดโปรไฟล์หน้าตั้งค่า (`components/EmailVerifyState.ts`
     + `components/EmailVerifyBadge.vue` + `views/SettingsView.vue`) — เดิมมีสองสถานะ ทำให้ "ยังไม่ได้ผูกอีเมล" กับ
     "ผูกแล้วแต่ยังไม่ยืนยัน" หน้าตาเหมือนกัน ⇒ ผู้ใช้กดปุ่มผิดตัว
   · 🟢 UI · `components/PlanDoneToggle.vue` แทนติ๊กถูกเดิมในการ์ดแผนวันนี้
     (ทั้งฝั่งมื้ออาหารและฝั่งการฝึก) — ตัวเดิมสถานะเพี้ยน/กดแล้วไม่รู้ว่าติดหรือไม่ติด
   · 🔧 ด่านสองตัวที่แดงเฉพาะบน Windows เรื่องพาธ (`scripts/verify-wear.mjs` ·
     `scripts/verify-i18n.mjs`) — ไม่กระทบพฤติกรรมแอป แต่ทำให้เกตรันได้จริงบนเครื่องเจ้าของ

   ⚠️ สคริปต์ migration ชุด REST อยู่ **นอก** `iufit-vue` ⇒ ไม่อยู่ในบันเดิลนี้
   ⚠️ ป้ายเจ้าของเครื่องที่ค้างที่ uid เก่า เว็บรอบนี้มีตัวย้ายในตัว (`queueHandover`/
      `runHandover` ใน `services/firebase.ts` + สาขาเขียนทับ `ownerUid` ใน `claimOwnership()`)
      ⇒ ผู้ใช้ที่เปิดเว็บใหม่ไม่ต้องรอ migration
   ⚠️ **สิ่งที่เว็บรอบนี้ยังแก้ไม่ได้เอง**: โค้ชเขียน `clients/$dev/plan` / `removed` ให้ลูกเทรน
      ที่ยังไม่เคยเปิดเว็บใหม่ — กฎตัดสินจาก `clients/$dev/coachUid` ซึ่ง **เขียนได้เฉพาะ
      เครื่องลูกเทรน** (`memSelfSync()` ใน `services/coachClient.ts`) ⇒ ต้องรอ migration เติมย้อนหลัง
      หรือรอให้ลูกเทรนเปิดแอปหนึ่งครั้ง · ดู `01-เอกสาร-แผน-dev/ยิง-deploy-เว็บ-ตอนนี้-2569-08-03.md` §3.1

   · 🔧 `scripts/verify-docrefs.mjs` — ปิดจุดบอดของตัวด่านเอง (ชื่อไฟล์ที่ถูกตัดกลางด้วย `…`
     ถูกอ่านเป็นไฟล์ผี · ตัวกรอง `…` เดิมไม่เคยทำงานเลยเพราะ `…` ไม่อยู่ในชุดตัวอักษรของ `REF_RE`)
     + self-test 12 เคสที่ล้มถ้าใครผ่อนตัวกรองจนด่านตาบอด

   · ✅ เกตปิดรอบ: `verify:all` **40/40 ด่าน · ตก 0** · `vue-tsc --noEmit` สะอาด ·
     `verify:i18n` th 3,436 = en 3,436 · ทั้ง 10 หัวข้อเป็น 0 ·
     `verify:contrast --gate` ผ่าน ([T] 191/191 · [G] 149/149 · [U] 0/0) ·
     `verify:rules` 143 เคส ตก 0 (รันนิพจน์จากไฟล์กฎ GOLIVE ตัวจริง) ·
     golden 21 ไฟล์ · 485 เคส · ตก 0 */
/* v1187 · 🕐 คีย์วันต้องเป็น "วันของผู้ใช้" ไม่ใช่วันของ UTC + ด่านที่ทนการย้ายไฟล์
   ─────────────────────────────────────────────────────────────────────────────
   · 🐛 `views/PrivacyView.vue` — ชื่อไฟล์ที่ผู้ใช้ดาวน์โหลดจากหน้า "สิทธิ์ของคุณ"
     (`iufit-my-data-YYYY-MM-DD.json` และ `iufit-iu-mate-unanswered-…`) เคยตัดวันจาก
     `new Date().toISOString().slice(0, 10)` = **UTC** ⇒ คนไทย (UTC+7) ที่กดส่งออก
     ก่อน 07:00 น. ได้ไฟล์ชื่อ "เมื่อวาน" ⇒ ไฟล์สำรองเรียงผิดวันทั้งโฟลเดอร์
     (บั๊กชนิดเดียวกับที่ `services/coachExport.ts` ปิดไปแล้ว) ⇒ ใช้ `dstr()` ทั้งสองจุด

   · 🔧 `scripts/verify-persistence.mjs` §P1-13 — **บทเรียนเรื่องด่านที่ผูกกับชื่อไฟล์**
     ข้อเดิมอ่านแค่ `src/views/CoachView.vue` แล้วหา `const today = dstr()` พอการ์ด
     "งานวันนี้" ถูกแยกไป `components/CoachTodayCard.vue` (โค้ดใหม่ยังใช้ `dstr()` ถูกต้อง)
     ด่านก็แดงทันที · ถ้าแก้ด้วยการชี้ไฟล์ใหม่แบบตายตัวก็จะพังซ้ำในการย้ายครั้งถัดไป
     ⇒ เขียนใหม่ให้กวาด `src/` ทั้งต้นไม้ (362 ไฟล์) ไม่อ้างพาธไหนเลย:
       (ก) คีย์วันทุกจุดห้ามคำนวณจาก `toISOString()`
       (ข) ทั้ง src/ ห้ามมี `toISOString().slice|substr|substring(0, 10)` ในโค้ดจริง
       (ค) ต้องยังหาโค้ดฝั่งโค้ชที่ถือคีย์วันเจอ (กันด่านผ่านเพราะไม่เจออะไรเลย)
       (ง) ไฟล์กลุ่มนั้นทุกใบต้องได้คีย์วันจาก `dstr(`
     พิสูจน์แล้ว 4 ทาง: ใส่ `toISOString()` กลับใน `CoachTodayCard.vue` → แดง 3 ข้อ ·
     คืน `PrivacyView` เป็นของเดิม → แดงข้อ (ข) · ย้ายไฟล์ไป
     `src/features/coach/today/TodayTasks.vue` (สำเนาทดลอง) → **ยังเขียว** ·
     ถอดคีย์วันฝั่งโค้ชออกจนหมด → แดงข้อ (ค)

   · ✅ เกตปิดรอบ: `verify:all` **40/40 ด่าน · ตก 0** · `vue-tsc --noEmit` สะอาด ·
     `verify:persistence` 1,094 ข้อ ตก 0 · `verify:i18n` th 3,458 = en 3,458 ·
     `verify:contrast --gate` ผ่าน ([T] 191 · [G] 149 · [U] 0) ·
     golden 21 ไฟล์ · 485 เคส · ตก 0 */
/* v1188 · 🔴 **รอบปิดคืน 7 สาย (2569-08-04)** — "แอปเปิดตาให้ตัวเอง + เลิกพูดสิ่งที่ไม่จริง"
   ─────────────────────────────────────────────────────────────────────────────
   คืนนี้มี 7 สายทำงานขนานกันโดยมองไม่เห็นกัน · **68 ไฟล์ใน `src/` เปลี่ยน** และเกือบทั้งหมด
   เป็นโค้ดที่ผู้ใช้เห็นผลตรง ๆ ⇒ ไม่ขยับเลขนี้ = เครื่องที่ติดตั้งไว้แล้วยังกิน SW เก่าที่เสิร์ฟ
   `index.html` ซึ่งอ้าง hash ของก้อน asset ชุดเดิม = ทุกอย่างข้างล่างนี้ไปไม่ถึงผู้ใช้เลย

   งานที่ลงในรอบนี้:
   · 🔴 **กล่องดำ** (`services/diag.ts` + `main.ts` + `pwa.ts` + `services/analytics.ts`)
     ทั้งรีโปเคยไม่มี `window.onerror` / `unhandledrejection` / `app.config.errorHandler`
     **เลยสักจุด** ⇒ บั๊กทุกตัวถูกสืบจากคำบอกเล่า · ตอนนี้ดัก 3 ชั้น · กรอง PII 10 กฎก่อน
     เขียนลงดิสก์ · มีเพดานรายการ+ไบต์ (ยอมสละที่ให้ข้อมูลจริงก่อนเสมอ) · ลบพร้อมบัญชี
     และ **ไม่ติดไปกับไฟล์สำรองของผู้ใช้** · ด่าน `verify.diag.ts` ลงทะเบียนแล้วในรอบนี้
   · 🔴 **กระเป๋าเครดิต AI พูดตรงความจริง** (`services/ai.ts` + `stores/food.ts` +
     `FoodView` / `FoodLogView` / `SettingsView` / `IngredientFormSheet`)
     gym proxy ตอบ 503 (`wallet_unavailable`/`proxy_down`) เวลาอ่านกระเป๋าไม่ได้ แต่แอปมี
     `catch` เดียวที่แปลทุกสาเหตุเป็น **"🔒 ยืนยันอีเมลเพื่อรับเครดิต AI ฟรี"** ⇒ คนที่ยืนยัน
     อีเมลไปแล้วและ**ซื้อเครดิตไปแล้ว**เห็นข้อความนี้ แล้วอาจเชื่อว่าเครดิตหาย = จ่ายซ้ำ
     ⇒ แยกเป็นสถานะที่สาม "อ่านยอดไม่ได้ชั่วคราว — ลองใหม่อีกครั้ง" (th+en) โดย **สิทธิ์
     ไม่เปลี่ยน** (ยัง locked · ไม่เดายอดเป็นเลข) · ด่านใหม่ `verify.wallet.ts`
   · 🟢 แผนฝึก / cardio / การบ้าน (`stores/workoutPlan.ts` · `views/CardioView.vue` ·
     `components/TodayPlanWorkout.vue` · `HwReviewSheet` · `stores/hwSubmit.ts`)
   · 🟢 ความเป็นส่วนตัว (`views/PrivacyView.vue` · `services/backup.ts` · `views/BackupView.vue`)
   · 🟢 IU Mate (`lib/iuMateBot.ts` · `stores/iumate.ts` · `components/IuMateSheet.vue`)
   · 🔴 ป้ายเตือนของแพ้ (`AllergyWarnBadge` · `AllergyChips` · `AllergySettings` ·
     `composables/useAllergyRecipe.ts`) + ด่านโครงสร้าง `verify-allergy-ui.mjs`
   · 🔴 เงินจริง (`line-oa/billing*.js|html` + `scripts/verify-billing-guard.mjs §27`)
     — อ่านกระเป๋าไม่ได้ห้ามทับของจริงทิ้ง · `/credit/add` idempotent ด้วย `chargeId`

   🔴🔴 **และบั๊กที่ใหญ่กว่าทุกข้อข้างบน — ไฟล์นี้เองแปลไม่ผ่านมาตั้งแต่ v1182**
   ─────────────────────────────────────────────────────────────────────────────
   บันทึกรอบ v1182 (2569-08-02) ยก **ตัวปิดคอมเมนต์บล็อก** มาเขียนไว้ในเนื้อความ
   ตอนอธิบายบั๊ก "คอมเมนต์ CSS ถูกอ่านเป็นโค้ด" — ตัวปิดนั้นไป **ปิดคอมเมนต์ของ
   changelog เอง** ⇒ ร้อยแก้วภาษาไทยที่เหลือกลายเป็นโค้ด ⇒ **ทั้งไฟล์เป็น SyntaxError**
   (สองจุด · เดิมอยู่บรรทัด 408 และ 413 · แก้แล้วในรอบนี้)

   ทำไมเงียบมาสองวัน:
     · ไฟล์นี้ **ไม่ผ่านบันเดิลเลอร์** — `vite build` ก๊อปทั้งไฟล์ตามตัวอักษรลง `dist/`
       ⇒ `vue-tsc`/`vite` ไม่เคย parse มันเลย (`dist/sw.js` ที่ deploy ไปก็พังเหมือนกัน)
     · ด่าน `verify-pwa` เดิมถามแค่ "มีสตริงนี้ในไฟล์ไหม" ⇒ ผ่านสบายกับไฟล์ที่พัง
     · เบราว์เซอร์ไม่ส่งเสียง — `register()` ล้มใน console ของ SW ซึ่งคนละที่กับ console ของหน้า
   ⇒ ผลจริง: ตั้งแต่ v1182 **การบัมป์เลข sw ไม่ทำอะไรเลย** เพราะ SW ตัวใหม่ติดตั้งไม่ขึ้น
     ออฟไลน์ไม่ทำงาน · เครื่องเดิมยังกินแคชชุดเก่าต่อไป (นี่คือสิ่งที่ v1183/v1187
     ตั้งใจจะแก้แล้วคิดว่าแก้แล้ว)
   ⇒ ตั้งด่านกันซ้ำ: `verify-pwa` คอมไพล์ `public/sw.js` ด้วย `node:vm` ทุกครั้ง
     (พิสูจน์แล้ว: ใส่ตัวปิดคอมเมนต์กลับเข้าไป → ด่านแดงทันที)

   ⚠️ ด่านสองตัวที่ "เขียนไว้แล้วแต่ไม่มีใครรัน" ถูกลงทะเบียนในรอบนี้: `verify.diag.ts`
      และ `verify.wallet.ts` (ทั้ง `package.json` และ `scripts/verify-all.mjs`)
      ⇒ ชุดกลางเพิ่มจาก 40 → **42 ด่าน**

   · ✅ เกตปิดรอบ (รันในแซนด์บ็อกซ์ Linux · 2569-08-04):
     `verify:all` **44 ด่าน → ผ่าน 42 · ตก 1 · TIMEOUT 1** ·
     `vue-tsc --noEmit` สะอาด · golden 23 ไฟล์ · **487 เคส · ตก 0** ·
     `verify:contrast --gate` ผ่าน ([T] 191/191 · [G] 149/149 · [U] 0/0) ·
     i18n key parity ตรวจแยก: th 3,465 = en 3,465 · ขาดสองทาง 0 · ไทยค้างใน en 0

     🟠 ที่ตก 1 = `verify-deploy-gate` ข้อ G3.1 — `dist/sw.js` ยังเป็น **v1187** ขณะที่
        `public/sw.js` เป็น v1188 · **นี่คือด่านทำงานถูก ไม่ใช่บั๊ก**: บิลด์ยังไม่ได้รัน
        (`npm run build` ต้องรันบน Windows — rollup ในรีโปนี้เป็นไบนารี win32)
        ⇒ หายเองทันทีที่ build · `deploy_vue.py` เรียกเกตนี้ **หลัง** build เสมอ
     ⏱ ที่ TIMEOUT 1 = `verify:i18n` — เกินเพดาน 45 วิ/คำสั่งของแซนด์บ็อกซ์
        (ตัวมันเดินทีละอักขระทั่ว `src/` 362 ไฟล์ + ยิงลูก 4 โปรเซสฉาก boot-race)
        **ห้ามอ่านว่าผ่าน** — ต้องรันบน Windows ก่อน deploy */
/* ── v1189 · 2569-08-04 ────────────────────────────────────────────────────────
   ตัดวงกลมของเกตก่อน deploy · เก็บงานหลัง v1188

   1. `verify:all` เรียกเกตแบบเต็ม (รวมข้อ "อะไร live") แต่ `deploy_vue.py`
      รัน `verify:all` เป็นด่านแรก ⇒ **การยกเว้น `--surface=web` ไม่มีผลเลย**
      เพราะถูกบล็อกตั้งแต่ชั้นนอก · แยกเป็น `--scope=code|deploy|all` แล้ว
      (ทะเบียนกลาง 18 ข้อ · self-test พิสูจน์ทุกครั้งว่า code ∪ deploy = ทั้งหมด)
   2. `verify:i18n` ค้างไม่จบบน Windows — regex หาลิเทอรัลมีสองสาขาซ้อนกัน
      (`[^\`]` กิน `\` ได้) ⇒ catastrophic backtracking 2ⁿ · จุดชนวนคือ backtick
      ตัวเดียวใน regex ของ `services/diag.ts` ที่ตัวตัดคอมเมนต์มองไม่เห็น
      ⇒ ไม่จบ → **1.6 วิ** · ทุกขั้นพิมพ์ความคืบหน้า + ฉากบูตมีเพดานเวลาแล้ว
   3. manifest มีสถานะที่สาม "⛔ ไม่ได้ deploy โดยตั้งใจ" (พร้อมเหตุผล + แฮชผูก)
      ⇒ เลิกบีบให้คนตอบคำถามที่ไม่มีคำตอบถูกด้วย `--record-deployed=all-current`

   ⚠️ เลข sw ต้องขึ้นเสมอ ห้ามลดหรือเท่าเดิม — ชื่อซ้ำ = ไบต์เหมือนของที่เครื่อง
      ผู้ใช้ถืออยู่ = `install` ไม่ทำงาน = ค้างถาวร (บทเรียนจาก v1182 ที่ไฟล์นี้
      เป็น SyntaxError อยู่ 2 วันโดยไม่มีใครรู้ · ตอนนี้ `verify-pwa` คอมไพล์จริง) */
/* ── v1190 · 2569-08-04 ────────────────────────────────────────────────────────
   การ์ดแชร์ส่งออกเพี้ยน + ปิดช่องโหว่ claim ให้ถาวร

   1. 🔴 `html2canvas` จับภาพอิลิเมนต์ที่อยู่ใต้ `transform: scale()` ⇒ เอา
      **ขนาดกรอบ** จาก `getBoundingClientRect()` (โดนย่อ) แต่เอา **ขนาดตัวอักษร**
      จาก computed style (ไม่โดนย่อ) ⇒ ผืนผ้าใบ 450×800 แทน 2160×3840
      ⇒ ตัวอักษรใหญ่กว่าช่องที่มันควรอยู่ 4.80 เท่า · `77` ซ้อนกัน **69%**
      ⇒ แก้ด้วยการจับภาพสำเนาในกล่องนอกจอที่ไม่มี transform
      (ถอด transform ของตัวจริงไม่ได้ — ทดลองแล้วได้รูปว่างเปล่า/ค้าง >45 วิ)
   2. ปุ่มแชร์เงียบ — เรนเดอร์ใช้ ~2.2 วิ แต่ `userActivation` หมดที่ ~5 วิ
      ⇒ บนมือถือ `share()` โยน `NotAllowedError` แล้วถูกกลืนเป็น "บันทึกแล้ว"
      ⇒ สาขา `cancelled` ของทั้งสองชีตเป็นบล็อกว่าง `{ }` = เงียบสนิท
      ⇒ เก็บ blob ไว้ให้แตะครั้งที่สองข้ามการเรนเดอร์ + ทุกเหตุผลต้องมีคนพูด
   3. wordmark 39 → 60 px (เท่า `.cs-logo` ของการ์ด 1080×1920 อีกใบ)
      ⚠️ สีน้ำเงินแบรนด์ `--text-brand` ใช้ไม่ได้บนพื้นการ์ด — วัดได้ **1.42:1**
         (เกณฑ์การ์ดแชร์ ≥7:1) ⇒ คงล็อกอัพขาว/เขียว 12.00:1 · 7.11:1 ไว้
   4. ทะเบียน `scripts/must-be-gone.json` — "ไฟล์ที่ต้องไม่มีอยู่บนของจริงแล้ว"
      🔴 ที่มา: `deploy_vue.py` วางทับอย่างเดียว **ไม่เคยลบ** ⇒ `claim.html`
      ที่ลบจากรีโปไปเมื่อวาน **ยังเสิร์ฟอยู่บน iufit.com** และยังยิงไป
      `/claim?email=` ที่ยังเปิดอยู่ = แจกรหัสเปิดสิทธิ์ให้คนที่รู้อีเมลคนอื่น
      ⇒ เกตถามเพิ่มว่า "ของจริงเสิร์ฟอะไรอยู่" ไม่ใช่แค่ "เราจะวางอะไร"

   ⚠️ เลข sw ต้องขึ้นเสมอ ห้ามลดหรือเท่าเดิม (บทเรียน v1182 · ดู v1189) */
const CACHE = 'iufit-v1191-line-lang-sync'

/* เปลือกแอปที่ชื่อไฟล์คงที่ (ชื่อ hash ของ /assets/* เก็บตอน runtime แทน)

   path เป็น root-absolute (`/…`) ไม่ใช่ `./…` แบบ sw.js ตัวเดิม เพราะแอปนี้เป็น history-mode
   SPA: เวลา SW ทำงานอยู่ path `./index.html` จะถูก resolve เทียบกับ scope ซึ่งพาไปผิดที่เมื่อ
   ผู้ใช้เปิดจาก URL ลึก ๆ อย่าง /coach/clients — `/index.html` ชี้ที่เดียวเสมอ */
const FILES = [
  '/',
  '/index.html',
  '/manifest.json',
  '/icon-192.png',
  '/icon-512.png',
  '/apple-touch-icon.png',
]

/**
 * ⚡ 2569-07-26 · อ่านชื่อก้อน `/assets/*` จาก index.html แล้ว precache ตั้งแต่ตอน install
 * ══════════════════════════════════════════════════════════════════════════════
 * เดิมก้อนโค้ดถูกแคชแบบ "เจอตอนไหนค่อยเก็บ" ⇒ ครั้งแรกหลังปล่อยเวอร์ชันใหม่ ผู้ใช้ต้อง
 * ยิงเน็ตทีละก้อนแบบต่อคิว (ก้อนหลัก → ก้อนของหน้า → ก้อนที่หน้านั้นเรียกต่อ) กว่าจะเห็นจอ
 *
 * ทำไมอ่านจาก HTML แทนที่จะฮาร์ดโค้ดรายชื่อ: ชื่อไฟล์มี hash ต่อท้ายและเปลี่ยนทุก build
 * รายชื่อที่เขียนมือจะล้าสมัยเงียบ ๆ ในวันที่ลืมอัปเดต แล้วกลายเป็น precache ของที่ไม่มีจริง
 * index.html คือแหล่งความจริงเดียวที่บอกว่า build นี้ต้องใช้ก้อนไหน
 *
 * ⚠️ ดึงเฉพาะก้อนที่ index.html อ้างตรง ๆ (ก้อนหลัก + vendor + css) ไม่ไล่ทั้งกราฟ
 * ก้อนของแต่ละหน้ายังโหลดตอนเข้าหน้านั้น — precache ทั้งแอปจะกินเน็ตผู้ใช้เพื่อหน้าที่เขาไม่เปิด
 */
async function precacheShell(c) {
  try {
    // `cache: 'reload'` — ต้องได้ของสดจากเซิร์ฟเวอร์ ไม่ใช่ของเก่าใน HTTP cache ของเบราว์เซอร์
    const res = await fetch('/index.html', { cache: 'reload' })
    if (!res || !res.ok) return
    const html = await res.clone().text()
    await c.put('/index.html', res.clone())
    await c.put('/', res)
    const urls = [
      ...new Set(
        [...html.matchAll(/(?:src|href)="(\/assets\/[^"]+\.(?:js|css))"/g)].map((m) => m[1]),
      ),
    ]
    await Promise.allSettled(urls.map((u) => c.add(u)))
  } catch {
    /* เน็ตล่มตอน install = ไม่ต้อง precache · fetch handler ยังเก็บให้ตอนใช้จริงอยู่ดี */
  }
}

/* resilient install: ไฟล์เดียวหาย/404 ต้องไม่ทำให้อัปเดตทั้งชุดพัง (allSettled ไม่ใช่ all) */
self.addEventListener('install', (e) => {
  e.waitUntil(
    caches.open(CACHE).then(async (c) => {
      await Promise.allSettled(FILES.map((f) => c.add(f)))
      await precacheShell(c)
    }),
  )
  self.skipWaiting()
})

self.addEventListener('activate', (e) => {
  e.waitUntil(
    caches.keys().then((ks) => Promise.all(ks.filter((k) => k !== CACHE).map((k) => caches.delete(k)))),
  )
  self.clients.claim()
})

self.addEventListener('fetch', (e) => {
  if (e.request.method !== 'GET') return
  const url = new URL(e.request.url)
  const sameOrigin = url.origin === self.location.origin
  /* history-mode SPA: /food, /coach/... มาเป็น mode==='navigate' ทั้งหมด → เข้าทาง network-first
     และตอนออฟไลน์ตกมาที่ ./index.html ให้ router เดินเส้นทางต่อเองฝั่ง client */
  /* 🐛 2569-07-28 · **แยกหน้าเอกสารออกจาก app shell** (P0 · รายงานตรวจโค้ด 28 ก.ค.)
     ═══════════════════════════════════════════════════════════════════════════
     เงื่อนไขเดิมถือว่า "ทุก navigation = หน้าแอป" ⇒ เกิดสองปัญหาพร้อมกัน:

       1. **เสิร์ฟผิดหน้า** — สาขา HTML ด้านล่างเป็น *cache-first* ⇒ เปิด
          `/privacy.html` แล้วได้ `/index.html` จากแคชกลับไป**ทันที** โดยไม่ดู path
          (แรงกว่าฝั่ง deploy ที่เป็น network-first ด้วยซ้ำ — อันนั้นยังไปถามเน็ตก่อน)
       2. **เขียนทับเชลล์** — response ของหน้าเอกสารถูก `put('/index.html')`
          ⇒ เชลล์ในแคชกลายเป็นหน้านโยบายถาวร

     แยกสามทาง (กติกาเดียวกับ `IUFIT-app-deploy/sw.js` — ห้ามให้สอง SW คิดไม่ตรงกัน):
       · เชลล์จริง   `/` หรือ `/index.html`      → เขียนทับเชลล์ได้
       · เส้นทาง SPA  navigate ที่ไม่มีนามสกุล    → ใช้เชลล์ตอบ แต่ **ไม่เขียนแคช**
       · หน้า `.html` เดี่ยว                      → network-first เก็บใต้คีย์ตัวเอง */
  const swIsShell = (pn) => pn === '/' || pn.endsWith('/index.html')
  const hasExt = /\.[a-z0-9]{1,8}$/i.test(url.pathname)
  const nav = e.request.mode === 'navigate'
  const isHTML = sameOrigin && (swIsShell(url.pathname) || (nav && !hasExt))
  const isDoc = sameOrigin && /\.html?$/i.test(url.pathname) && !swIsShell(url.pathname)

  /* หน้าเอกสารเดี่ยว (privacy · terms · account-delete · …) — network-first
     ออฟไลน์แล้วไม่มีสำเนา ⇒ ปล่อยเป็น network error ตามจริง
     **ห้าม**ตกมาที่ `/index.html` — คนที่ขอหน้านโยบายไม่ควรได้แอปทั้งตัว */
  if (isDoc) {
    e.respondWith(
      fetch(e.request)
        .then((n) => {
          if (n && n.ok) {
            const cl = n.clone()
            e.waitUntil(caches.open(CACHE).then((c) => c.put(e.request, cl)))
          }
          return n
        })
        .catch(() => caches.match(e.request)),
    )
    return
  }

  /**
   * ⚡ 2569-07-26 · HTML เปลี่ยนจาก network-first → **แคชก่อน แล้วอัปเดตเบื้องหลัง**
   * ══════════════════════════════════════════════════════════════════════════════
   * ของเดิมรอเน็ตครบรอบก่อนโค้ดบรรทัดแรกจะได้รัน — **ทุกครั้งที่เปิดแอป**
   * สมัยเป็นไฟล์เดียว รอบนั้นคุ้ม เพราะมันส่งทั้งแอปมาเลย
   * ตอนนี้ index.html เหลือ 12KB ที่ไม่มีเนื้อหาอะไร รอบนั้นจึงเป็นการรอเปล่า ๆ
   * ก่อนงานจริงจะเริ่ม · แอป Android ยิ่งหนักเพราะ `server.url` ชี้ที่ iufit.com
   * ⇒ เปิดแอปทุกครั้งวิ่งผ่านเน็ต ไม่ได้อ่านจากเครื่อง
   *
   * ทำไมของเก่าไม่ค้าง: index.html กับก้อน `/assets/*` อยู่ใน **แคชชื่อเดียวกัน**
   * และถูกลบพร้อมกันตอน activate ⇒ HTML ที่เสิร์ฟจากแคชอ้างถึงก้อนที่อยู่ในแคชนั้นเสมอ
   * ไม่มีทางเกิดกรณี "HTML เก่าชี้ไปก้อนที่ถูกลบแล้ว"
   * และเมื่อมี SW ใหม่ `skipWaiting + claim` จะยิง controllerchange ให้หน้ารีโหลดเอง
   * (ดู src/pwa.ts) ⇒ อย่างช้าที่สุดคือช้าไปหนึ่งการเปิดแอป แล้วตามทันเอง
   */
  if (isHTML) {
    e.respondWith(
      caches.match('/index.html').then((cached) => {
        const fresh = fetch(e.request)
          .then((n) => {
            /* ⚠️ `swIsShell` ตรงนี้คือหัวใจ — ห้ามถอด
               เส้นทาง SPA เซิร์ฟเวอร์ส่ง index.html กลับมาเหมือนกันก็จริง
               แต่วันไหนส่ง 404 มาแทน จะทับเชลล์ด้วยหน้า 404 = บั๊กเดิมในรูปแบบใหม่ */
            if (n && n.ok && sameOrigin && swIsShell(url.pathname)) {
              const c1 = n.clone()
              const c2 = n.clone()
              caches.open(CACHE).then((c) => {
                c.put('/index.html', c1)
                c.put('/', c2)
              })
            }
            return n
          })
          .catch(() => null)
        // มีของในแคช = ตอบทันที แล้วปล่อยให้อัปเดตวิ่งต่อเบื้องหลัง
        // `waitUntil` กัน SW ถูกฆ่ากลางคันก่อนเขียนแคชเสร็จ (ไม่งั้นจะไม่มีวันได้ของใหม่)
        if (cached) {
          e.waitUntil(fresh)
          return cached
        }
        return fresh.then((n) => n || caches.match('/index.html'))
      }),
    )
    return
  }

  e.respondWith(
    caches.match(e.request).then(
      (r) =>
        r ||
        fetch(e.request)
          .then((n) => {
            if (n && n.ok && sameOrigin) {
              const cl = n.clone()
              caches.open(CACHE).then((c) => c.put(e.request, cl))
            }
            return n
          })
          /* ⚠️ เดิมปิดท้ายด้วย `.catch(() => caches.match('/index.html'))`
             ⇒ `.js`/`.css` ที่โหลดไม่ได้จะได้ **HTML** กลับไป ⇒ เบราว์เซอร์พาร์ส
             เป็นสคริปต์แล้วพังด้วย SyntaxError ที่ไล่ต้นตอไม่เจอ
             โหลดไม่ได้ต้องเป็นโหลดไม่ได้ — เงียบกว่านั้นคือโกหก */,
    ),
  )
})

/* Web push (FCM) — พอร์ตตรงจาก sw.js ของจริง ห้ามตัดทิ้ง ไม่งั้น noti ที่ส่งอยู่แล้วเงียบหาย */
self.addEventListener('push', function (e) {
  let d = {}
  try {
    d = e.data ? e.data.json() : {}
  } catch (_) {
    try {
      d = { body: e.data.text() }
    } catch (__) {
      d = {}
    }
  }
  const title = d.title || 'IUFIT 💪'
  const opts = {
    body: d.body || '',
    icon: d.icon || '/icon-192.png',
    badge: '/icon-192.png',
    tag: d.tag || 'iufit',
    renotify: true,
    data: { url: d.url || '/' },
  }
  e.waitUntil(self.registration.showNotification(title, opts))
})

/* ── pushsubscriptionchange — กันแจ้งเตือน "เงียบหายเอง" ────────────────────────────
   เบราว์เซอร์หมุน/ยกเลิก subscription ได้เองโดยที่ผู้ใช้ไม่ได้ทำอะไร (อัปเดตเบราว์เซอร์ ·
   push service เปลี่ยน endpoint ฯลฯ) เมื่อเกิดขึ้น endpoint เดิมบนเซิร์ฟเวอร์กลายเป็นขยะ
   และแจ้งเตือน **เงียบไปตลอดกาลทั้งที่สวิตช์ในแอปยังเขียวอยู่** = ปุ่มหลอกที่เกิดทีหลัง

   SW สมัครใหม่ให้ได้ แต่ **เขียน Firebase เองไม่ได้** (ไม่มี token ผู้ใช้ในนี้) จึงส่งของใหม่
   ไปให้หน้าเว็บเป็นคนบันทึก — ครึ่งหลังอยู่ที่ `installPushSubscriptionSync()` ใน
   services/push.ts · ถ้าตอนนั้นไม่มีแท็บเปิดอยู่ ข้อความจะตกหาย แต่ `pushRefresh()`
   ตอนบูตครั้งถัดไปจะเขียนทะเบียนล่าสุดทับให้เองอยู่แล้ว ⇒ ไม่มีทางค้างถาวร

   ⚠️ ใช้ applicationServerKey ตัวเดิมจาก subscription เก่า — VAPID public key ไม่ถูก
   ฮาร์ดโค้ดซ้ำในไฟล์นี้ (และ private key ไม่เคยอยู่ฝั่ง client อยู่แล้ว) */
self.addEventListener('pushsubscriptionchange', function (e) {
  e.waitUntil(
    (async function () {
      try {
        var old = e.oldSubscription || (await self.registration.pushManager.getSubscription())
        var key = old && old.options && old.options.applicationServerKey
        if (!key) return
        var fresh =
          e.newSubscription ||
          (await self.registration.pushManager.subscribe({
            userVisibleOnly: true,
            applicationServerKey: key,
          }))
        if (!fresh) return
        var j = fresh.toJSON()
        /* คีย์ของทะเบียนเก่า คำนวณด้วยสูตรเดียวกับ blob `_pushKey()` — ต้องส่งไปด้วย
           ไม่งั้นหน้าเว็บลบของเก่าไม่ถูก แล้วตัวส่งจะยิงไป endpoint ที่ตายแล้วตลอดไป */
        var oldKey = null
        if (old && old.endpoint) {
          var h = 0
          for (var i = 0; i < old.endpoint.length; i++) {
            h = ((h << 5) - h + old.endpoint.charCodeAt(i)) | 0
          }
          oldKey = 'k' + (h >>> 0).toString(36)
        }
        var cl = await self.clients.matchAll({ type: 'window', includeUncontrolled: true })
        for (var n = 0; n < cl.length; n++) {
          cl[n].postMessage({
            type: 'iufit:push-resubscribed',
            endpoint: j.endpoint,
            keys: j.keys || {},
            oldKey: oldKey,
          })
        }
      } catch (_) {
        /* สมัครใหม่ไม่ได้ (สิทธิ์ถูกถอน ฯลฯ) — เงียบไว้ ไม่มีอะไรที่ SW ทำต่อได้
           หน้าเว็บจะเห็นสถานะจริงเองตอนเปิดครั้งถัดไปผ่าน pushRefresh() */
      }
    })(),
  )
})

self.addEventListener('notificationclick', function (e) {
  e.notification.close()
  const target = (e.notification.data && e.notification.data.url) || '/'
  e.waitUntil(
    clients.matchAll({ type: 'window', includeUncontrolled: true }).then(function (cl) {
      for (let i = 0; i < cl.length; i++) {
        if (cl[i].url.indexOf(self.location.origin) >= 0) {
          try {
            cl[i].focus()
          } catch (_) {}
          try {
            if (target && target !== '/' && 'navigate' in cl[i]) cl[i].navigate(target)
          } catch (_) {}
          return
        }
      }
      if (clients.openWindow) return clients.openWindow(target)
    }),
  )
})
